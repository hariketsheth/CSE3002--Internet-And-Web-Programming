<?php
  function test{
  $name = readline("Enter the name: ");
  $principal_amt = (int) readline("Enter the Prinicpal Amount the user have: ");
  $salary_ceil = 20000;
  $age = (int) readline("Enter the age of the user: ");
  $monthly_salary = readline("Enter the monthly salary of the Employee: ");
    foreach($meployees as $employee){
        $employee['principal_amt'] += $principal_amt;
        $employee['monthly_incone'] += $monthly_salary;
        $employee['']= 0;
        //Initialising the array for reference in the later part
    }

    while($i>=$n){
        $employee[10][5] = array();
        $decided_amt = readline("Enter the amount decided by recruiter and the company: ");
        $final_amt = ($principal_amt * $rate * $time )/ 100 ;
        $rate = 20;
        while($i<$n){
            $total['amount'] * $rate * $time;
            function check($numbers){

            }
        }
    }
    }
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exercise 2 - Lab 10 - PHP</title>
    <link rel="stylesheet" href="style.css" class="rel">
</head>
<body>
    <form action="" method="POST">
        <input type="text" class="text" placeholder="Enter the name of the Student">
        <input type="number" class="number1" placeholder="Enter the numbers obtained by the Student">
        <input type="number" class="number2" placeholder="The average marks are:: ">
        <label for="text" class="text"></label>
        <label for="number1" class="number1"></label>
        <label for="number2" class="number2"></label>
        <?php 
        function check($subjects){
            foreach($subjects as $sub)

        }
        ?>
</form>
</body>
</html>